【Step4｜Design Validation｜${FORMATION_TARGET}】

Step対象：
Design_Validation.md

Pipeline：
${PIPELINE}

Formation対象：
${FORMATION_TARGET}

Target Module：
${TARGET_MODULE}

Foundation：
${FOUNDATION}

━━━━━━━━━━━━━━━━━━━

Discoveryは禁止します。

Builder Input Sheet作成は禁止します。

Builder成果物の修正は禁止します。

改善案は禁止します。

推測は禁止します。

Builder成果物をObservationしてください。

━━━━━━━━━━━━━━━━━━━
■ Formation対象

${FORMATION_TARGET}｜${TARGET_MODULE} Foundation｜${FOUNDATION}

━━━━━━━━━━━━━━━━━━━
■ Purpose

Builder成果物が、

Builder Input Sheetのみを根拠としてFormationされ、

${TEMPLATE_STRUCTURE}のTemplate Structureを維持しているかObservationしてください。

Design ValidationではBuilder成果物は修正しません。

━━━━━━━━━━━━━━━━━━━
■ 使用可能入力

① ${TEMPLATE_STRUCTURE}

② Builder Input Sheet｜${FORMATION_TARGET}

③ Builder成果物｜${FORMATION_TARGET}

━━━━━━━━━━━━━━━━━━━
■ Design Validation責務

Design Validationでは、

Builder成果物が、

・Builder Input SheetのみをFormation材料としているか

・${TEMPLATE_STRUCTURE}のTemplate Structureを維持しているか

・Builder責務を超えていないか

のみObservationしてください。

改善案は禁止します。

Builder成果物の修正は禁止します。

推測は禁止します。

━━━━━━━━━━━━━━━━━━━
■ 確認対象

① Template Structure整合

確認：

Builder成果物は、

${TEMPLATE_STRUCTURE}の章構成・章順・見出し・本文構造を維持してFormationされているか。

━━━━━━━━━━━━━━━━━━━

② Formation材料整合

確認：

Builder Input SheetのFormation材料のみでFormationされているか。

Builder Input Sheetに存在しない内容を追加していないか。

━━━━━━━━━━━━━━━━━━━

③ Builder責務整合

確認：

Builder責務を超える判断・追加・削除・設計変更を行っていないか。

━━━━━━━━━━━━━━━━━━━

④ Formation範囲整合

確認：

BuilderがFormation対象のみをFormationしているか。

━━━━━━━━━━━━━━━━━━━

⑤ 停止有無

確認：

Builder成果物をReviewへ渡すことを停止すべき事実があるか。

━━━━━━━━━━━━━━━━━━━
■ 禁止事項

❌ Builder成果物修正

❌ Builder Input Sheet修正

❌ Repository更新

❌ Review

❌ Implementation

❌ 改善案

❌ Solution形成

❌ 推測

━━━━━━━━━━━━━━━━━━━
■ 返却形式

① Template Structure整合

② Formation材料整合

③ Builder責務整合

④ Formation範囲整合

⑤ 停止有無
