【Foundation Progress｜更新依頼】

Step対象：
Foundation_Progress.md

Pipeline：
${PIPELINE}

Formation対象：
${FORMATION_TARGET}

Maturity対象：
${MATURITY_TARGET}

Target Module：
${TARGET_MODULE}

Foundation：
${FOUNDATION}

━━━━━━━━━━━━━━━━━━━

現在module：

${TARGET_MODULE}

対象：

${FOUNDATION}

状態：

Implementation Validation完了
（Repository反映確認完了）

目的：

foundation_progress.md を現在状態へ更新する。

Foundation Progress更新後、
更新後のfoundation_progress.mdをObservationし、
Pipeline順に次Formation対象を返却する。

重要：

進捗更新のみ。

形成内容変更は禁止。

Foundation Progress責務：

Foundation Progress更新後、
foundation_progress.md本文をObservationし、
Pipeline順に次Formation対象を返却する。

次Formation対象はDecisionではなくObservationとして扱う。

foundation_progress.md本文から確認できる内容のみ返却する。

━━━━━━━━━━━━━━━━━━━

使用可能入力

・Implementation Validation返却｜${FORMATION_TARGET}

禁止：

❌ Foundation本文変更

❌ 他Module更新

❌ Maturity更新

❌ Repository構造変更

❌ 改善案

❌ 推測

━━━━━━━━━━━━━━━━━━━

返却形式

① 更新内容

② 変更ファイル

③ 差分要約

④ 次Formation対象

Observation：

現在のfoundation_progress.md上、

Pipeline順で次に開始すべきFormation対象

理由

⑤ 停止有無
