【Step3｜Builder｜${FORMATION_TARGET}】

Step対象：
Builder.md

Pipeline：
${PIPELINE}

Formation対象：
${FORMATION_TARGET}

Target Module：
${TARGET_MODULE}

Foundation：
${FOUNDATION}

━━━━━━━━━━━━━━━━━━━

Discoveryは禁止します。

Builder Input Sheet以外は禁止します。

推測は禁止します。

Builder Input SheetのみをFormation材料として使用してください。

━━━━━━━━━━━━━━━━━━━
■ Formation対象

${TEMPLATE_STRUCTURE}のTemplate Structureを完成させてください。

Builderは、

${TEMPLATE_STRUCTURE}の各章・各見出しに対応するBuilder Input SheetのFormation材料のみを差し込みます。

Builder Input Sheetに存在しない情報は追加しません。

Builder Input Sheet上で「Formation材料なし」「確認不可」とされた章は、

その旨を本文内へ反映してください。

━━━━━━━━━━━━━━━━━━━
■ Purpose

Builder Input SheetをFormation材料として使用し、
${TEMPLATE_STRUCTURE}をTemplate Structureとして${FORMATION_TARGET}をFormationしてください。

BuilderはBuilder Input Sheet以外のFormation材料を使用しません。

Builderは${TEMPLATE_STRUCTURE}を章構成・章順・見出し・本文構造のTemplateとしてのみ使用します。

━━━━━━━━━━━━━━━━━━━
■ 使用可能入力

① ${TEMPLATE_STRUCTURE}

② Builder Input Sheet｜${FORMATION_TARGET}

━━━━━━━━━━━━━━━━━━━
■ Builder責務

Builderでは、

${TEMPLATE_STRUCTURE}をTemplate Structureとして使用し、

Builder Input SheetをFormation材料として、

${FORMATION_TARGET}をFormationしてください。

Builderは${TEMPLATE_STRUCTURE}の章構成・章順・見出し・本文構造を変更しません。

Builderは${TEMPLATE_STRUCTURE} Template Structureを完成形として再現します。

Builder Input Sheetは章を作るための入力ではありません。

Builder Input Sheetは、

${TEMPLATE_STRUCTURE} Template Structureの各章へ差し込むFormation材料です。

Builderは、

${TEMPLATE_STRUCTURE}に存在する章のみを出力します。

Builderは、

Builder Input Sheetに存在するFormation材料を、

対応する章へ配置します。

Builder Input Sheetに存在しない内容は追加しません。

Builder Input Sheet上で確認不可とされた内容は、

その章内で確認不可として記述します。

新しい章・見出しは作成しません。

Formation対象成果物（${FORMATION_TARGET}.md）はBuilder入力として使用しません。

Repository本文は使用しません。

Discovery Observationは使用しません。

推測は禁止します。

━━━━━━━━━━━━━━━━━━━
■ Formation対象

以下をFormationしてください。

- Module Name
- Target Module
- Role
- Responsibility
- Module Purpose
- Module Self Recognition
- Module SoT
- Module Repository
- Module Input
- Module Output
- Product Owner Boundary
- Codex Boundary
- General Design GPT Boundary
- Knowledge Boundary
- Authority Boundary
- Non Responsibility
- Connected Modules
- Completion Criteria

${TEMPLATE_STRUCTURE}構造を維持し、

${TARGET_MODULE}固有内容のみを反映してください。

━━━━━━━━━━━━━━━━━━━
■ 禁止事項

❌ Discovery Observation使用

❌ Repository本文使用

❌ Formation対象成果物（${FORMATION_TARGET}.md）使用

❌ Design Validation

❌ Review

❌ Implementation

❌ Implementation Validation

❌ Prototype Validation

❌ Repository更新

❌ Solution形成

❌ 改善案

❌ 推測

❌ Template変更

❌ ${TEMPLATE_STRUCTURE}に存在しない章の追加

❌ ${TEMPLATE_STRUCTURE}に存在しない見出しの追加

❌ Builder判断による章構成変更

━━━━━━━━━━━━━━━━━━━
■ 返却形式

① Formation内容

② 変更ファイル

③ 差分要約

④ Formation範囲

⑤ 停止有無
