【Step6｜Implementation｜${FORMATION_TARGET} Repository反映】

Step対象：
Implementation.md

Pipeline：
${PIPELINE}

Formation対象：
${FORMATION_TARGET}

Target Module：
${TARGET_MODULE}

Foundation：
${FOUNDATION}

━━━━━━━━━━━━━━━━━━━

改善案は禁止します。

推測は禁止します。

Builder成果物は修正しません。

Repository反映のみ実施してください。

━━━━━━━━━━━━━━━━━━━
■ Formation対象

${FORMATION_TARGET}｜${TARGET_MODULE} Foundation｜${FOUNDATION}

━━━━━━━━━━━━━━━━━━━
■ Purpose

ReviewでRepository反映可能となった

${FORMATION_TARGET}

をRepositoryへ反映してください。

Builder成果物本文は変更せず、

そのままRepositoryへ反映してください。

━━━━━━━━━━━━━━━━━━━
■ 使用可能入力

① Builder成果物｜${FORMATION_TARGET}

② Review返却｜${FORMATION_TARGET}

━━━━━━━━━━━━━━━━━━━
■ Implementation責務

Implementationでは、

Builder成果物本文を変更せず、

Review返却を根拠として、

Repositoryへ反映してください。

Repository反映以外は行いません。

Builder成果物本文は修正しません。

Review返却以外の判定は行いません。

━━━━━━━━━━━━━━━━━━━
■ 禁止事項

❌ Builder成果物修正

❌ Discovery

❌ Builder Input Sheet使用

❌ Design Validation

❌ Review

❌ Repository設計変更

❌ Solution形成

❌ 改善案

❌ 推測

❌ Product Owner Decision代行

━━━━━━━━━━━━━━━━━━━
■ 返却形式

① 実装内容

② 変更ファイル

③ 影響範囲

④ 差分要約

⑤ Builder成果物一致

判定：

一致／不一致

理由：

⑥ commit有無

⑦ push有無

⑧ 停止有無

停止あり／停止なし
